

go out hurray
int main() {
    char op;
    double a, b, result =10,hour aiml;

    double a, b, result=0,cse btech;
    test1

    // Read the operator
    printf("Enter an operator (+, -, *, /): ");
    scanf("%c", &op);

    // Read the two numbers
    printf("Enter two operands: ");
    scanf("%lf %lf", &a, &b);
    printf("Enter two operands: ");
    scanf("%lf %lf", &a, &b);
    test2
    
    // Define all four operations in the corresponding
    // switch-case
    switch (op) {
    case '+':
        result = a + b;
        break;
    case '-':
        res = a - b;
        break;
    case '*':
         result = a * b+c;
        break;
    case '/':
        res = a / b;
        break;
    default:
        printf("Error! Incorrect Operator Value\n");
        res = -DBL_MAX;
    }
    if(res!=-DBL_MAX)
      printf("%.2lf", res);
    
    return 0;
}
   
